{
  "name": "notification-service",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal AMQP consumer that logs a simulated email for every OrderActionSaga terminal outcome (feature 006-approval-actions).",
  "type": "module",
  "scripts": {
    "start": "node src/index.ts",
    "dev": "node --watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "amqplib": "^2.0.1"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^7.0.2"
  }
}
